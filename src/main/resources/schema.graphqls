type Query {
    users: [User]
    user(id: Int!): User
    cars: [Car]
    car(id: Int!): Car
    me: User
}

type User {
    id: ID!
    name: String!
    vehicles: [Car]
}

type Car {
    id: ID!
    make: String!
    model: String!
    color: String!
    owner: User
}